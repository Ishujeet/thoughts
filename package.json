{
  "name": "thoughts",
  "version": "0.1.0",
  "description": "Attach a shared, git-backed brain to every repo in a multi-repo project.",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "thoughts": "./dist/index.js"
  },
  "files": [
    "dist",
    "templates",
    "kit",
    "LICENSE",
    "NOTICE"
  ],
  "engines": {
    "node": ">=22"
  },
  "os": [
    "darwin",
    "linux"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && chmod +x dist/index.js && mkdir -p dist/brain/backends/schema && cp src/brain/backends/schema/pg.sql dist/brain/backends/schema/pg.sql && cp src/brain/backends/schema/nebula.ngql dist/brain/backends/schema/nebula.ngql",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "handlebars": "^4.7.8",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "optionalDependencies": {
    "pg": "^8.16.0",
    "tree-sitter-wasms": "0.1.13",
    "web-tree-sitter": "0.25.10"
  }
}
